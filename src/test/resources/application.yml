spring:
  profiles:
    active: test


gleif-endpoints:
  uri: https://api.gleif.org/api/v1/lei-records
  pagination-uri: https://api.gleif.org/api/v1/lei-records?page[size]=pageSize&page[number]=pageNumber

gleif-appconfig:
  page-size: 250
